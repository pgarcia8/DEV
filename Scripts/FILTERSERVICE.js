 var serviceList = aa.env.getValue("MultiService");var countryList=["MARION_COUNTY"];//add county agency list herevar contryServiceList=serviceList.clone();var cityServiceList = serviceList.clone();contryServiceList.clear();for (var i =0;i<serviceList.size();i++){	var service = serviceList.get(i);	for(var j=0;j<countryList.length;j++)	{		if(service.getServPorvCode().equals(countryList[j]))		{			contryServiceList.add(service);			cityServiceList.remove(service);		}	}}var returnServiceList = cityServiceList.clone();var needReturn = "";for(var i=0;i<contryServiceList.size();i++){	var countryService = contryServiceList.get(i);	needReturn = "true";	        for(var j=0;j<cityServiceList.size();j++)	{		var cityService=cityServiceList.get(j);		if(cityService.getServiceName().equals(countryService.getServiceName()))		{			needReturn="false";			break;		}	}	if(needReturn=="true")	{		returnServiceList.add(countryService);	}}aa.env.setValue("returnService",returnServiceList);aa.env.setValue("countryServiceList",contryServiceList);aa.env.setValue("cityServiceList",cityServiceList);