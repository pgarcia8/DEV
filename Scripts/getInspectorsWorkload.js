 Script Initializer://set scheduledDate var scheduledDate = aa.date.getCurrentDate();scheduledDate.setMonth(6);//0-11scheduledDate.setDayOfMonth(8);scheduledDate.setHourOfDay(16);Script Text:// Enter your script here...//DEMO for ReSchedulevar my_array = new Array();my_array[0]=29041;//inspection id numberid1 = "08777";id2 = "00000";id3 = "00007";capObj = aa.cap.getCapID(id1,id2,id3).getOutput();rs= aa.inspection.getInspectorsWorkload(my_array,capObj,scheduledDate);if(rs.getSuccess()){    aa.print("Get Inspectors For Workload successfully");    userlist= rs.getOutput();    size=userlist.size();    aa.print("Get " + size + " workload users " );    if(userlist!=null && !userlist.isEmpty()){        var it=userlist.iterator();        while(it.hasNext())        {            var userModel=it.next();            //aa.print("ServiceProviderCode="   + userModel.getServiceProviderCode());            //aa.print("FullName="             + userModel.getFullName())            //aa.print("UserID="                + userModel.getUserID());         }    }}else{    aa.print("Get workload Inspectors failed.");}//end for ReSchedule//DEMO for Schedulevar my_array = new Array();my_array[0]=84002188;//inspection id numbercapObj = null;rs= aa.inspection.getInspectorsWorkload(my_array,capObj,scheduledDate);if(rs.getSuccess()){    aa.print("Get Inspectors For Workload successfully.");    userlist= rs.getOutput();    size=userlist.size();    aa.print("Get " + size + " workload users " );    if(userlist!=null && !userlist.isEmpty()){        var it=userlist.iterator();        while(it.hasNext())        {            var userModel=it.next();            aa.print("ServiceProviderCode="   + userModel.getServiceProviderCode());            aa.print("FullName="             + userModel.getFullName())            aa.print("UserID="                + userModel.getUserID());         }    }}else{    aa.print("Get workload Inspectors failed.");}